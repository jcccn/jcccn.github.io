extends includes/layout.pug

block content
  .inner
    h2= page.title
    if page.type == 'tags'
      if site.tags && site.tags.length
        != tagcloud()
        h3 Tag List
        != list_tags()
        != page.content
    else 
      != page.content
